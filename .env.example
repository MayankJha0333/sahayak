# ---- Firebase (Project settings → Your apps → Web app → SDK config) ----
EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=

# OAuth Web client id (Authentication → Google → Web SDK configuration). Android Google Sign-In needs it.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=

# Region your functions are deployed in
EXPO_PUBLIC_FUNCTIONS_REGION=asia-south1

# Point the app at local emulators instead of the cloud (true/false)
EXPO_PUBLIC_USE_EMULATORS=false
# Optional: your computer's Wi-Fi IP (run `ipconfig getifaddr en0`). Only needed for a real iPhone on the emulators;
# Android phones find the computer by themselves.
# EXPO_PUBLIC_EMULATOR_HOST=192.168.1.5

# ---- Razorpay (public key only; the secret lives in functions/.env) ----
EXPO_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxx

# Only needed for the real Google map on Android
EXPO_PUBLIC_GOOGLE_MAPS_KEY=
# Expo Go on Android shows OpenStreetMap maps instead (its Google map is blank). Set to 1 to use them everywhere.
# EXPO_PUBLIC_WEB_MAPS=1

# Link added to the "Share with neighbours" message on the coming-soon page (Play Store / website)
# EXPO_PUBLIC_SHARE_URL=https://play.google.com/store/apps/details?id=com.sahayak.app
